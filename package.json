{
  "name": "hidrogrow",
  "version": "1.0.0",
  "private": true,
  "description": "HidroGrow — Aplikasi Manajemen Hidroponik Rumahan (full lokal)",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "test": "npm run test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
